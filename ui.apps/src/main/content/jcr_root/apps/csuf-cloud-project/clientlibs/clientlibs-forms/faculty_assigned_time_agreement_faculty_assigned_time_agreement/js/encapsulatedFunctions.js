
//Do not change this file.
var faculty_assigned_time_agreement_faculty_assigned_time_agreement = window.faculty_assigned_time_agreement_faculty_assigned_time_agreement = window.faculty_assigned_time_agreement_faculty_assigned_time_agreement || {};
/**
 * @function faculty_assigned_time_agreement_faculty_assigned_time_agreement.generated_SubmitFlag_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {string}
 */
faculty_assigned_time_agreement_faculty_assigned_time_agreement.generated_SubmitFlag_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if (StageIndicator.value === null) {\n    if (StartTerm.value !== null && EndTerm.value !== null) {\n        submit1600234699256.enabled = true;\n    }\n}";
            return eval(code);
        }
	}
}
