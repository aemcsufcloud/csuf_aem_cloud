
//Do not change this file.
var award_adjustment_appeal_award_adjustment_appeal = window.award_adjustment_appeal_award_adjustment_appeal = window.award_adjustment_appeal_award_adjustment_appeal || {};
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_NewAward1_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_NewAward1_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(FallAward1.value !== null && SpringAward1.value !== null){\nthis.value = (FallAward1.value) % (SpringAward1.value);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_FallAward1_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_FallAward1_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward1.value !== null){\nthis.value = (NewAward1.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward1_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward1_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward1.value !== null){\nthis.value = (NewAward1.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_FallAward2_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_FallAward2_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward2.value !== null){\nthis.value = (NewAward2.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward2_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward2_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward2.value !== null){\nthis.value = (NewAward2.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_FallAward3_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_FallAward3_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward3.value !== null){\nthis.value = (NewAward3.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward3_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward3_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward3.value !== null){\nthis.value = (NewAward3.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_FallAward4_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_FallAward4_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward4.value !== null){\nthis.value = (NewAward4.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
/**
 * @function award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward4_calc0
 * @this currentComponent
 * @param {scope} scope in which code inside function will be executed.
 * @returns {number}
 */
award_adjustment_appeal_award_adjustment_appeal.generated_SpringAward4_calc0 = function (scope) {
    with(this) {
        with(scope) {
            var code = "if(NewAward4.value !== null){\nthis.value = (NewAward4.value) / (2);\n}else{\n  this.value = 0;\n}";
            return eval(code);
        }
	}
}
